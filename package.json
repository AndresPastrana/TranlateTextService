{
  "name": "translategpt",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "node ./build/index.js",
    "test:watch": "bun test --watch --timeout 1000 --rerun-each 100",
    "build": "bun build --minify --splitting --target=node index.ts --outdir=./build"
  },
  "devDependencies": {
    "bun-types": "latest",
    "path-to-regexp": "6.2.1",
    "zod": "^3.22.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}